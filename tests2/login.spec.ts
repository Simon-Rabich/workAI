// import { test, expect } from '@playwright/test';
// import { LoginPage } from '../pages/LoginPage';

// test('Login via Login  with Exisitng email', async ({ page }) => {
//   const login = new LoginPage(page);
//   await login.gotoLoginPage();

//   // Send email in the email field (assumed to be an input element)
//   await page.locator('input[name="email"]').fill('simonravitz@gmail.com');
  
//   // Click on the Login button
//   await page.locator('button[type="button"] span:has-text("Login")').click();

//   // Verify the error message
//   // const errorMessage = await page.locator('.FormField__error'); // Assuming error message has the class FormField__error
//   // await expect(errorMessage).toHaveText('Error while authorizing');
// });
